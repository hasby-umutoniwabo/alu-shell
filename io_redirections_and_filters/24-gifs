#!/bin/bash
declare -A seen_dirs find . -type f -iname "*.gif" | while read -r f do dir="${f%/*}" name="${f##*/}" base_name="${name%.gif}" if [ -z "${seen_dirs[$dir]}" ] then seen_dirs[$dir]=1 printf "%s\n" "$base_name" fi done | sort -f
